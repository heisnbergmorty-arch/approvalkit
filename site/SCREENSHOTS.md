# Screenshots TODO

These show up on the landing page (site/index.html) and OG share cards.
Take all at 2880×1800 retina, then export 1440×900 PNG.

## 1. `hero-mockup.png`

The agency dashboard with 1 project showing "Awaiting client review" status,
brand color set to indigo. Clean inbox state. Use the seed data.

## 2. `client-portal.png`

The client review page (`/review/<slug>`) with 3 image assets in a grid.
Show one with status "approved" (green), one with "pending" (gray), and one
with "changes requested" (amber).

## 3. `pinned-comment.png`

Close-up of the asset-card with 2 pinned comments visible (numbered circles
on the image). Hover-tooltip showing one expanded.

## 4. `email-preview.png`

The "X approved Y" email rendered in a Mac Mail / iPhone Mail mockup.
Use a tool like https://mockuphone.com or Figma's iPhone frame.

## 5. `og-share.png`

Auto-generated by `app/src/app/opengraph-image.tsx`. Just verify it renders
correctly by opening `https://app.approvalkit.com/opengraph-image` after
deploy.
